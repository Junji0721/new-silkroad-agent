# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# プロジェクト規約

## 1. 基本対話ルール
- 返答、説明、コード内のコメントはすべて「日本語」で行ってください。
- ファイルを作成・変更する際は、実施する前に必ず変更内容の概要を提示してください。

## 2. 安全性・セキュリティ
- `.env` や鍵情報が含まれるファイルは絶対に読み込まないでください。
- `rm -rf` や `git reset --hard` などの破壊的コマンドは実行不可とします。
- 個人情報(氏名、メールアドレス、電話番号等)や機密情報を含むファイルは絶対に読み込まないでください。
- APIキー、パスワード、接続文字列などのハードコーディングは禁止します。必ず環境変数から参照するコードを書くこと。
- ファイルを削除する必要がある場合、`rm` 等で直接削除せず、プロジェクト直下の `.trash/` フォルダへ移動すること(元のディレクトリ構成が分かるよう、相対パスを保ったまま移動する)。ユーザーが完全削除を明示的に指示した場合を除く。

## 3. マスキング・ダミーデータ規定
- サンプルデータ・テストデータ・デモデータを生成する際は、実データや実在する個人情報を一切使用しないこと。
- 既存の本番データ・ログ・CSV・データベースの値をコピーしたり、一部だけ伏字にして「マスキング」として使い回すことは禁止する(部分的に実データが残るため不十分)。必ずゼロから完全に架空の値を生成すること。
- 氏名・住所・電話番号・メールアドレス等は、一目でダミーと分かる値を使用する(例: 氏名は「テスト太郎」、メールは `user@example.com`、電話番号は `090-0000-0000`)。
- 生成したダミーデータが偶然にも実在の人物・組織と一致する可能性に配慮し、疑わしい場合はより明確に架空と分かる値に修正すること。

## 4. コード品質・ディレクトリ
- 主要フォルダ構造:
  - `src/`: ソースコード
  - `docs/`: ドキュメント
- インデントはスペース2つとし、シンプルで可読性の高いコードを書くこと。
- コード変更後は、可能であれば動作確認コマンドを提案してください。

## 5. Pre-commitフック(シークレットスキャン)
- コミット前に [gitleaks](https://github.com/gitleaks/gitleaks) がステージ済みの差分をスキャンし、APIキーや秘密鍵などのシークレットを検出する。
- フック本体は `.githooks/pre-commit` にあり、リポジトリで共有される。クローン後は最初に1度だけ `git config core.hooksPath .githooks` を実行して有効化すること。
- gitleaks 本体はローカルにインストールが必要(`~/.local/bin/gitleaks` に配置済み)。未インストールの環境ではコミットがエラーで止まる。
- gitleaks は正規表現ベースのシークレット検出ツールであり、氏名・住所などの一般的な個人情報の検出は対象外。個人情報の混入防止は [2. 安全性・セキュリティ](#2-安全性セキュリティ)・[3. マスキング・ダミーデータ規定](#3-マスキングダミーデータ規定) のルールで対応する。

## 現在の状態

このプロジェクトは現時点でコード・設定・ドキュメントが一切存在しない空の状態です(`src/`・`docs/` フォルダも未作成)。このファイルはプレースホルダーです。

プロジェクトが進んだら、以下の内容でこのファイルを更新してください:
- ビルド・lint・テストの実行コマンド(単一テストの実行方法を含む)
- 構築され次第の全体的なアーキテクチャと構成
