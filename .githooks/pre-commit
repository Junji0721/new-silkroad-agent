#!/usr/bin/env bash
set -euo pipefail

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "エラー: gitleaks が見つかりません。インストールしてから再度コミットしてください。" >&2
  echo "  参照: https://github.com/gitleaks/gitleaks#installing" >&2
  exit 1
fi

gitleaks protect --staged --redact -v
